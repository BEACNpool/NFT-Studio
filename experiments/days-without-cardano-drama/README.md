# Days Without Cardano Drama

**Minted once on Cardano and delivered to $beacnleaks on September 8, 2026.**

[Play the NFT on pool.pm](https://pool.pm/asset1845ppy9k6ugyf6fjjkcmegy4gyxfrtjzc2tee7) · [BEACN showcase](https://beacnpool.github.io/NFT-Studio/showcase/days-without-cardano-drama/).

A counter reads 3 DAYS. A deadpan worker pulls the drama trigger. It drops to 0 DAYS with an original womp-womp. The final revision adds Cardano blue, official starburst geometry, an ADA badge, circuit motifs and a reset burst. The full animated artwork remains scalable vector code inside the program; the compact static cover is AVIF. This is satire, not a live incident tracker.

## Confirmed edition

- Policy: `98cd88d43153a8244eaaaeabada3a4022e8600812baec720470a7fa2`
- Asset name: `DaysWithoutCardanoDrama`, raw UTF-8 hex `44617973576974686f757443617264616e6f4472616d61`
- Transaction: `3683e58be5690b1d874fa9df0b835e14467ab2b7a663b1894cdc381d7c22bc39`
- Exactly +1 minted and delivered with 1.219730 ADA. Network fee 0.837821 ADA. Studio fee 0 ADA.
- Complete signed CBOR: 15,510 bytes. Exact chain CBOR, public-key signature, body/metadata hashes, asset/supply, destination and both reconstructed media passed independent checks.

The AVIF cover is 3,625 bytes, SHA256 `b113560143b30c4e264b7d684af801e9d26125c187f068b8c9de68f04d0cfe11`. The HTML is 8,264 bytes, SHA256 `e4a515b7b226f06d892480127ea57ec23e00eb2319bc7ede8254692e6e6b961f`. The full animation, original synthesizer and all vector geometry are embedded; browser-generated audio samples do not require a media service. Click Play to unlock sound; Replay resets local state.

The native policy permits authorized mint/burn until 17:40:35 UTC on September 8 (slot 197322944). The receipt observes supply 1; it does not claim a one-shot guarantee or prove policy closure. Do not resubmit the confirmed transaction or remint this edition.

## Rebuild and evidence

After normal repository dependency installation:

```sh
node experiments/days-without-cardano-drama/source/build.mjs /path/to/new-output-directory
```

This rebuilds the exact compressed program and reuses the frozen, hash-checked cover. `source/encode-cover.py` separately reproduces the AVIF from the included 720×820 raster master with its recorded FFmpeg command; the vector source is `source/scene.svg`. PNG and expanded HTML are editable/viewing sources, not extra on-chain files. Reproduction was byte-identical on the recorded tool versions.

`public/showcase/days-without-cardano-drama/mint-receipt.json` is the confirmed public chain receipt; `minted-metadata.json` is recovered chain metadata. `playback.json` records actual desktop/390px replay, mute, sound signal, reduced-motion and opaque sandbox checks. `audio-verification.json` covers the unchanged original synthesizer. `draft-v1-synthetic-sizing.json` is historical evidence for the cream-colored, unminted first draft, never a receipt for this edition.

The real local MCP packaged and verified the final 11,889 raw bytes (intent `76b3fa8a2173a3a0afd4e2ed9e5b8f755d36c889604036fe93d0efb962c5edb1`). Mint and delivery then used the explicitly authorized local CLI signing route with a separate recipient output. This does not validate the outstanding unattended Windows Codex or Eternl acceptance gates. Ordinary Studio HTML review is static.

Actual pool.pm playback passed at 16:49:13 UTC on 1440px and 390px layouts: exact delivered HTML, title/owner, real-click sound, 3→0, replay and mute; no overflow, app errors or embedded-program network requests. Real CDN headers and opaque sandbox were retained. See `pool-pm-verification.json`.

Cardano logo attribution and source: `CARDANO-ATTRIBUTION.md`. Original BEACN code/art is under the repository license; Cardano marks retain their owners’ rights.
