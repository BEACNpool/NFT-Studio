# Harmonic Machines — exact originals delta

The integrated originals live in `public/originals/` and `lib/capability-catalog.json`. Three originals, 21 byte-verified files. Keep them under Sound. All three compact programs are recovered from current mint metadata and equal the frozen source delivery; the offline expanded copy decompresses from those exact bytes. WAV and MIDI files are original local exports and are not additional on-chain payload.

Gallery integration: append records through the existing `normalize` function. Extend Entry with `fileBuilderSupported?: boolean`. In `canBuildCopy`, require `item.fileBuilderSupported !== false`. For these entries show **Explore music tools**, routing `onCreate('music')`, with help text **Compose and export inside this original. The Studio’s smaller Beat Lab is available for creating a new music NFT.** This is an originals integration, not a new mint template. Existing iframe `sandbox="allow-scripts allow-downloads"` and SHA-256-before-execution are appropriate; never add `allow-same-origin`. Program controls already implement composition, song codes, WAV and MIDI. No wallet or network is used inside the instruments.

Originals are 13,111–13,171 bytes of compact HTML; all exceed generic file-builder 12 KB allowance. Historical complete signed transactions were 15,741 / 15,746 / 15,762 bytes, not promises for new wallet transactions. Their original policies are closed. Do not route these records into generic copy minting.

`media` includes exact cover, compact and expanded HTML, MIDI, WAV, song code and source configuration. Download links can use existing verified file handling. Add wav=audio/wav and mid=audio/midi to MIME mapping if desired. Generated export files are large (~19 MB total) and should load only on explicit download; the instruments generate fresh audio locally.

Validation is in `docs/HARMONIC_VERIFICATION.json`. The current recovery was performed independently; full natural playback and export checks are preserved dated evidence from the original release. Do not claim a new live playthrough from this kit verification.
